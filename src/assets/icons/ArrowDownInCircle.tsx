import { ArrowDownCircle } from 'iconoir-react'
import React from 'react'

const ArrowDownInCircle = (props: React.ComponentProps<typeof ArrowDownCircle>) => (
  <ArrowDownCircle width={'24px'} {...props} />
)

export default ArrowDownInCircle
