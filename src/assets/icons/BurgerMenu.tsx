import { Menu } from 'iconoir-react'
import React from 'react'

const BurgerMenu = (props: React.ComponentProps<typeof BurgerMenu>) => (
  <Menu width={'24px'} {...props} />
)

export default BurgerMenu
