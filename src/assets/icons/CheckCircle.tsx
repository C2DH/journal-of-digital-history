import { CheckCircleSolid } from 'iconoir-react'
import React from 'react'

const CheckCircle = (props: React.ComponentProps<typeof CheckCircleSolid>) => (
  <CheckCircleSolid width={'24px'} {...props} />
)

export default CheckCircle
