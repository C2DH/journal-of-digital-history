import { Circle } from 'iconoir-react'
import React from 'react'

const RadioButtonUnchecked = (props: React.ComponentProps<typeof Circle>) => (
  <Circle width={'24px'} {...props} />
)

export default RadioButtonUnchecked
